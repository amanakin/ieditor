#ifndef TEXT_HEADER
#define TEXT_HEADER

#include <widget.h>
#include  <graphlib.h>

/*struct Text: public Widget {
    // size - height of each symbol
    Text(const char* str, const Vector2i& pos, int fontSize,
         const Color& color, const MLFont& font);

    void draw(MLTexture& texture, const Vector2i& abs)     override;
    
protected:
    MLText text;
};*/


#endif // TEXT_HEADER
