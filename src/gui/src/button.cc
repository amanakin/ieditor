// button.cc

#include "button.h"

//*************************************************************

/*WidgetRectangle::WidgetRectangle(const Vector2i& size, const Vector2i& pos, const Color& color) :
    Widget(size, pos, nullptr),
    IDrawableRectangle(color)
{}

WidgetPicture::WidgetPicture(const Vector2i& size, const Vector2i& pos, const MLPicture& picture) :
    Widget(size, pos, nullptr), 
    IDrawablePicture(picture)
{}*/

//*************************************************************

/*ButtonToMove::ButtonToMove(const Vector2i& size, const Vector2i& pos, Widget* movableWidget, const Color& bg) :
    Widget(size, pos),
    Movable(movableWidget),
    DrawableRectangle(bg),
    TestableRectangle()
{}*/
